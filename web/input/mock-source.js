/**
 * web/input/mock-source.js — fake input: wraps mocks/keyboard-emitter.js
 * Owner: P4 (Integration)
 *
 * The other half of the mock toggle. Same surface as hub-source.js so the rest
 * of the app cannot tell hardware from keyboard. Enables full game dev with NO
 * board attached (?mock=1).
 *
 * SCAFFOLD STUB — TODO expose: MockSource.start(onMessage)
 *  - start the keyboard emitter, forwarding its Contract-1 events to onMessage
 */
