/**
 * web/config.js — app-level runtime config (no logic)
 * Owner: P4 (Integration)
 *
 * Single place for environment knobs so feature files never edit each other for
 * settings. Read by main.js and input sources.
 *
 * SCAFFOLD STUB — TODO define (as a global CONFIG object):
 *  - HUB_URL        : ws:// address of hub/server.js
 *  - MOCK           : derived from ?mock=1 query param (use keyboard emitter)
 *  - WIN_SCORE      : points to win a race
 *  - RAPID_FIRE_SEC : rapid-fire window length
 */
