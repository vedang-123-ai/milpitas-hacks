{
  "name": "dotrace-content-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build-time LLM generator for DotRace curriculum. Uses the OpenAI API + dotenv to read the local .env. Run `npm install` then `npm run generate`.",
  "scripts": {
    "generate": "node generate-content.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.67.0"
  }
}
