# Claude Code local files
.claude/
CLAUDE.md

# Node
node_modules/
npm-debug.log*

# Secrets — API keys live here, never committed
.env
.env.*
# ...but DO commit the template (no real key in it)
!.env.example

# OS
.DS_Store
